import { Link } from 'react-router-dom'
import { entrepreneurs } from '../../data/dummyData'
export default function ProductCard({ product }) { const maker = entrepreneurs.find(item => item.id === product.entrepreneurId); return <article className="overflow-hidden rounded-2xl border border-orange-100 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl hover:shadow-orange-100"><img src={product.image} alt={product.name} className="h-56 w-full object-cover" /><div className="p-5"><p className="text-xs font-bold uppercase tracking-wider text-orange-600">{product.category}</p><h3 className="mt-1 font-bold text-stone-900">{product.name}</h3><p className="mt-1 text-sm text-stone-500">by {maker?.name}</p><div className="mt-5 flex items-center justify-between"><span className="text-lg font-bold text-stone-900">₹{product.price.toLocaleString('en-IN')}</span><Link to={`/entrepreneurs/${product.entrepreneurId}`} className="text-sm font-bold text-orange-600 hover:text-orange-700">View Product →</Link></div></div></article> }
